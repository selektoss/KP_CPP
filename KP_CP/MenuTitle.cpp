#include "MenuTitle.hpp"

LoBject::LoBject(void)
{
	MenuWeight = lineCountTextHelp = sizeMenu = heightContent = weightContent = countColumnTitle = NULL;
	ArrLenTitleMenu = nullptr; minLenColumnSection = nullptr; Menu = nullptr; infoAddDelete = nullptr;
	coordXY = { 0,0 }; consoleATR = 0; nameSection = nullptr;
};

